# xinra [Prettier](https://prettier.io) configuration

**Install**:

```bash
$ npm i -D @xinra/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@xinra/prettier-config"
}
```
